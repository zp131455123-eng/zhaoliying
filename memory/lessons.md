# Lessons Learned - 踩坑教训

## 索引（按类别+条数+关键词）

| 类别 | 条数 | 关键词 |
|------|------|--------|
| 安全/密钥 | 1 | .env, 明文, 环境变量 |
| 时区/配置 | 1 | UTC, Asia/Shanghai, Cron |
| 记忆管理 | 1 | MEMORY.md, 归档, 150行 |
| 文档同步 | 1 | commit前检查, 文档一致性 |

---

## 类别1：安全/密钥

### 教训1: 密钥不能写明文

**原则**：
- 配置文件（TOOLS.md, config.json）只记录环境变量名
- 真实密钥只存在 `.env` 或系统环境变量中
- `.env` 必须在 `.gitignore` 第一行

**示例**：
```markdown
# ❌ 错误
API_KEY = "sk-1234567890abcdef"

# ✅ 正确
API_KEY = ${OPENAI_API_KEY}  # 环境变量
```

**教训**：
- **密钥泄露不可逆，预防为主**
- **commit 前必须检查敏感信息**

---

## 类别2：时区/配置

### 教训2: 时区必须明确标注

**现象**：
- Cron 任务执行时间不对
- 日志时间戳混乱

**原因**：
- Cron 默认使用 UTC 时区
- 不同系统/服务的默认时区不同

**解决方案**：
```bash
# Cron 配置（UTC 时区）
# 北京时间 08:00 = UTC 00:00
0 0 * * * /path/to/script.sh

# 脚本内部指定时区
export TZ="Asia/Shanghai"
```

**教训**：
- **所有时间配置必须标注时区（UTC / Asia/Shanghai）**
- **日志时间戳统一格式：YYYY-MM-DD HH:MM +TZ**

---

## 类别3：记忆管理

### 教训3: MEMORY.md 要保持精简

**原则**：
- MEMORY.md ≤ 150 行
- 只保留活跃项目和热记忆
- 超过 7 天且已完结的内容归档

**归档流程**：
1. 将完结项目移至 `memory/archive-index.md`
2. 将旧的"最近学习/发现"移至对应日志文件
3. 更新 MEMORY.md 的"最后更新"时间戳

**教训**：
- **热记忆要热，定期清理才能快速加载**
- **归档不是删除，是换个地方存（语义搜索仍能找到）**

---

## 类别4：文档同步

### 教训4: 改了架构必须同步文档

**场景**：
- 修改了 API 接口
- 调整了目录结构
- 更新了配置格式

**必须同步**：
- 项目的 README.md
- 相关的 API 文档
- MEMORY.md 的索引表格
- memory/projects.md 的项目状态

**教训**：
- **代码和文档要同步 commit**
- **commit 前自检：新会话的我能看懂吗？**

---

## 自检清单（commit 前必过）

- [ ] 敏感信息检查（密钥、个人信息）
- [ ] 时区标注检查（Cron、日志、配置）
- [ ] 文档同步检查（改了代码，文档更新了吗？）
- [ ] 归档检查（MEMORY.md 超过 150 行了吗？）

---

*最后更新: 2026-03-10 22:14*
